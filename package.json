{
  "name": "stripe-proxy",
  "version": "1.0.0",
  "description": "A simple proxy server for Stripe API access",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
